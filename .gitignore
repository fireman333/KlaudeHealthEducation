# Astro
dist/
.astro/

# Node
node_modules/

# Jekyll (legacy, kept until cleanup commit)
_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata
vendor/
Gemfile.lock
.bundle/

# Editor
.DS_Store
*.swp
.vscode/
.idea/

# Local-only
.env
.env.local
.env.*.local

# Quality gates (generated locally)
.lh-serve/
.lighthouseci/

# Cloudflare Pages deploy staging (dist wrapped under /klaudehealthedu)
.cf-deploy/

# Old GH Pages redirect site (generated; pushed to gh-pages branch)
redirect-site/

# D1 backups — contain user PII (emails, sessions). NEVER commit.
backup/
*.sql
